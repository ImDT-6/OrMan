<UserControl x:Class="OrMan.Views.Admin.KhoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1100">

    <UserControl.Resources>
        <ResourceDictionary Source="/Resources/InputStyles.xaml"/>
    </UserControl.Resources>

    <Grid Background="{StaticResource MainBackgroundGradient}">
        <Grid Margin="25,5,25,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="QUẢN LÝ KHO" FontSize="28" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Theo dõi nguyên liệu và định mức tồn kho" Foreground="#A0AEC0" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>
                <Button Command="{Binding AddCommand}" 
                        Content="+ Nhập Nguyên Liệu" 
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Style="{StaticResource GlassButtonStyle}" Width="180" Background="#22C55E"/>
            </Grid>

            <Border Grid.Row="1" Background="{StaticResource GlassBackground}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="20" Padding="20" Margin="0,0,0,20">
                <DataGrid ItemsSource="{Binding DanhSachNguyenLieu}" AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" BorderThickness="0" RowBackground="Transparent">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="TÊN NGUYÊN LIỆU" Binding="{Binding TenNguyenLieu}" Width="*" Foreground="White"/>
                        <DataGridTextColumn Header="ĐVT" Binding="{Binding DonViTinh}" Width="100" Foreground="#CBD5E1"/>

                        <DataGridTemplateColumn Header="TỒN KHO" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding SoLuongTon}" FontWeight="Bold" Foreground="{StaticResource WarningYellow}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="GIÁ VỐN" Binding="{Binding GiaVon, StringFormat=N0}" Width="150" Foreground="#CBD5E1"/>

                        <DataGridTemplateColumn Header="THAO TÁC" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="✏️ Sửa" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"
                                                Style="{StaticResource DataGridActionButtonStyle}" Background="#3B82F6" Margin="0,0,5,0"/>
                                        <Button Content="🗑️ Xóa" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"
                                                Style="{StaticResource DataGridActionButtonStyle}" Background="#EF4444"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</UserControl>