<Window x:Class="OrMan.Views.Admin.FeedBackWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Danh sách nhận xét khách hàng" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Phản hồi từ khách hàng" FontSize="20" FontWeight="Bold" Foreground="#004D8C" Margin="0,0,0,10"/>

        <DataGrid x:Name="dgFeedback" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Bàn" Binding="{Binding TableName}" Width="80"/>
                <DataGridTextColumn Header="Thời gian" Binding="{Binding CreatedDate, StringFormat='dd/MM/yyyy HH:mm'}" Width="150"/>

                <DataGridTextColumn Header="Đánh giá" Binding="{Binding StarDisplay}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#FFD700"/>
                            <Setter Property="FontSize" Value="16"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Nội dung nhận xét" Binding="{Binding Content}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>