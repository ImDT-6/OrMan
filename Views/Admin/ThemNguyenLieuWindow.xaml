<Window x:Class="OrMan.Views.Admin.ThemNguyenLieuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nguyên Liệu" Height="450" Width="400"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/InputStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <SolidColorBrush x:Key="InputBackground" Color="#20FFFFFF"/>

            <Style x:Key="ModernInputStyle" TargetType="{x:Type TextBox}">
                <Setter Property="Height" Value="45"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Padding" Value="15,0"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Background" Value="{StaticResource InputBackground}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TextBox}">
                            <Border x:Name="border" Background="{TemplateBinding Background}" 
                                    CornerRadius="12" BorderThickness="{TemplateBinding BorderThickness}" 
                                    BorderBrush="{TemplateBinding BorderBrush}">
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="#8B5CF6"/>
                                    <Setter TargetName="border" Property="Background" Value="#30FFFFFF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="20" Background="{StaticResource MainBackgroundGradient}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1">
        <Grid Margin="25">
            <StackPanel>
                <TextBlock x:Name="lblTitle" Text="THÊM NGUYÊN LIỆU" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

                <TextBlock Text="Tên nguyên liệu" Foreground="#A0AEC0" Margin="5,0,0,5"/>
                <TextBox x:Name="txtTen" Style="{StaticResource ModernInputStyle}" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Đơn vị tính" Foreground="#A0AEC0" Margin="5,0,0,5"/>
                        <TextBox x:Name="txtDVT" Style="{StaticResource ModernInputStyle}" Margin="0,0,0,15" ToolTip="kg, g, lít..."/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Giá vốn (VNĐ)" Foreground="#A0AEC0" Margin="5,0,0,5"/>
                        <TextBox x:Name="txtGia" Style="{StaticResource ModernInputStyle}" Margin="0,0,0,15"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Số lượng tồn kho ban đầu" Foreground="#A0AEC0" Margin="5,0,0,5"/>
                <TextBox x:Name="txtTonKho" Style="{StaticResource ModernInputStyle}" Margin="0,0,0,15"/>

                <TextBlock Text="Cảnh báo khi dưới mức (Tối thiểu)" Foreground="#A0AEC0" Margin="5,0,0,5"/>
                <TextBox x:Name="txtMin" Style="{StaticResource ModernInputStyle}" Margin="0,0,0,25" Text="0"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Content="Hủy" Click="BtnCancel_Click" Style="{StaticResource GlassDismissButtonStyle}" Height="45"/>
                    <Button Content="Lưu Lại" Click="BtnSave_Click" Grid.Column="2" Style="{StaticResource GlassButtonStyle}" Background="{StaticResource AccentGradient}" Height="45"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</Window>