<Window x:Class="GymManagement.Views.Admin.ThemSuaMonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Thông Tin Món Ăn" Height="550" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/InputStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="{StaticResource MainBackgroundGradient}" CornerRadius="20" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Tiêu đề -->
                <RowDefinition Height="*"/>
                <!-- Form nhập -->
                <RowDefinition Height="Auto"/>
                <!-- Nút bấm -->
            </Grid.RowDefinitions>

            <!-- 1. Tiêu đề -->
            <TextBlock x:Name="txtTitle" Text="THÊM MÓN MỚI" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <!-- 2. Form Nhập -->
            <StackPanel Grid.Row="1">
                <!-- [MỚI] Chọn Loại Món -->
                <TextBlock Text="Loại Món:" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
                <ComboBox x:Name="cboLoaiMon" Height="40" Margin="0,0,0,15" Padding="10,0" VerticalContentAlignment="Center" FontSize="14">
                    <ComboBoxItem Content="Mì Cay &amp; Lẩu" Tag="Mì Cay" IsSelected="True"/>
                    <ComboBoxItem Content="Đồ Chiên &amp; Topping" Tag="Đồ Chiên"/>
                    <ComboBoxItem Content="Nước Uống" Tag="Nước Uống"/>
                </ComboBox>

                <!-- Mã Món -->
                <TextBox x:Name="txtMaMon" ToolTip="Mã món (VD: MC001)" Style="{StaticResource GlassTextBox}" Margin="0,0,0,15"/>

                <!-- Tên Món -->
                <TextBox x:Name="txtTenMon" ToolTip="Tên món ăn" Style="{StaticResource GlassTextBox}" Margin="0,0,0,15"/>

                <!-- Giá Bán -->
                <TextBox x:Name="txtGiaBan" ToolTip="Giá bán (VNĐ)" Style="{StaticResource GlassTextBox}" Margin="0,0,0,15"/>

                <!-- Đơn Vị -->
                <TextBox x:Name="txtDonVi" ToolTip="Thành phần" Style="{StaticResource GlassTextBox}" Margin="0,0,0,15"/>

                <!-- URL Ảnh -->
                <TextBox x:Name="txtHinhAnh" ToolTip="Đường dẫn ảnh (/Images/...)" Style="{StaticResource GlassTextBox}" Margin="0,0,0,15"/>
            </StackPanel>

            <!-- 3. Nút Bấm -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Lưu Lại" Click="btnSave_Click" Style="{StaticResource GlassButtonStyle}" Width="120" Background="{StaticResource SuccessGreen}" Margin="0,0,10,0"/>
                <Button Content="Hủy Bỏ" Click="btnCancel_Click" Style="{StaticResource GlassButtonStyle}" Width="120" Background="{StaticResource DangerRed}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>