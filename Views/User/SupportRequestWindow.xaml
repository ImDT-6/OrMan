<Window x:Class="OrMan.Views.User.SupportRequestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Dịch Vụ Khách Hàng" 
        Width="480" 
        SizeToContent="Height" 
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/InputStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
                <Setter Property="Background" Value="#30000000"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="{StaticResource GlassBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="15,10"/>
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="35"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Name="PART_EditableTextBox" Style="{x:Null}" Background="Transparent" Foreground="White" BorderThickness="0" Margin="10,10,0,0" Padding="{TemplateBinding Padding}" VerticalContentAlignment="Center" HorizontalAlignment="Stretch"/>
                                    <ToggleButton Grid.Column="1" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                        <ToggleButton.Template>
                                            <ControlTemplate>
                                                <Border Background="Transparent">
                                                    <Path Data="M0,0 L5,5 L10,0" Stroke="White" StrokeThickness="2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                                <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                    <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border x:Name="DropDownBorder" Background="#2D3748" BorderThickness="1" BorderBrush="{StaticResource GlassBorder}" CornerRadius="10"/>
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="ItemContainerStyle">
                    <Setter.Value>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Padding" Value="12,10"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBoxItem">
                                        <Border x:Name="bd" Background="Transparent" CornerRadius="5" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentGradient}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ChipStyle" TargetType="RadioButton">
                <Setter Property="Background" Value="#15FFFFFF"/>
                <Setter Property="Foreground" Value="#DDDDDD"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="6"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="18" Padding="16,10">
                                <ContentPresenter HorizontalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="bdr" Property="Background" Value="#F59E0B"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="{StaticResource MainBackgroundGradient}" CornerRadius="20" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" Margin="0">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,25">
                <TextBlock Text="BẠN CẦN GIÚP GÌ?" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#F59E0B" BlurRadius="15" Opacity="0.4"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock Text="Vui lòng chọn hoặc nhập yêu cầu bên dưới" Foreground="{StaticResource TextSecondary}" FontSize="14" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <Border Grid.Row="1" Background="{StaticResource GlassBackground}" CornerRadius="18" Padding="25" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1">
                <StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="💬" FontSize="20" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="Gửi lời nhắn tới nhân viên" Foreground="White" FontWeight="Bold" FontSize="15" VerticalAlignment="Center"/>
                    </StackPanel>

                    <ComboBox x:Name="cboMessage" 
                              IsEditable="True" 
                              Style="{StaticResource DarkComboBoxStyle}"
                              Height="45"
                              Margin="0,0,0,15"
                              Text="Xin thêm nước lọc">
                        <ComboBoxItem Content="Mật khẩu wifi"/>
                        <ComboBoxItem Content="Xin thêm nước lọc"/>
                        <ComboBoxItem Content="Xin thêm chén bát"/>
                        <ComboBoxItem Content="Xin thêm đá lạnh"/>
                        <ComboBoxItem Content="Cần tư vấn menu"/>
                        <ComboBoxItem Content="Xin khăn giấy"/>
                        <ComboBoxItem Content="Gọi quản lý"/>
                    </ComboBox>

                    <Button Content="Gửi Yêu Cầu Hỗ Trợ" 
                            Click="BtnSupport_Click" 
                            Style="{StaticResource GlassButtonStyle}" 
                            Background="#203B82F6" 
                            Height="45" FontSize="14" FontWeight="Bold"/>


                    <Border Height="1" Background="{StaticResource GlassBorder}" Margin="10,25,10,30" Opacity="0.4"/>


                    <TextBlock Text="Chọn hình thức thanh toán:" Foreground="White" FontWeight="Bold" FontSize="15" Margin="0,0,0,15"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                        <RadioButton x:Name="radTienMat" Content="💵 Tiền mặt" IsChecked="True" Style="{StaticResource ChipStyle}"/>
                        <RadioButton x:Name="radCK" Content="📱 Chuyển khoản" Style="{StaticResource ChipStyle}"/>
                        <RadioButton x:Name="radThe" Content="💳 Thẻ" Style="{StaticResource ChipStyle}"/>
                    </StackPanel>

                    <Button x:Name="BtnCheckout" 
                            Content="GỬI YÊU CẦU THANH TOÁN" 
                            Click="BtnCheckout_Click" 
                            Style="{StaticResource GlassButtonStyle}" 
                            Background="{StaticResource WarningYellow}" 
                            Height="50" FontSize="14" FontWeight="Bold">
                        <Button.Effect>
                            <DropShadowEffect Color="#F59E0B" BlurRadius="15" ShadowDepth="0" Opacity="0.4"/>
                        </Button.Effect>
                    </Button>

                    <TextBlock Text="*Nhân viên sẽ mang phiếu tạm tính ra bàn" Foreground="{StaticResource TextSecondary}" FontSize="11" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,12,0,0"/>

                </StackPanel>
            </Border>

            <StackPanel Grid.Row="2" Margin="0,20,0,0">
                <Border Height="1" Background="{StaticResource GlassBorder}" Margin="20,0,20,15" Opacity="0.3"/>
                <Button Content="Đóng" Click="BtnCancel_Click" 
                        Style="{StaticResource GlassDismissButtonStyle}" 
                        Background="#15FFFFFF" Foreground="White" Height="40" Width="140" FontWeight="SemiBold" FontSize="14"/>
            </StackPanel>

        </Grid>
    </Border>
</Window>