<Window x:Class="OrMan.Views.User.GioHangWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Giỏ Hàng" Height="600" Width="500"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/InputStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="{StaticResource MainBackgroundGradient}" CornerRadius="20" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="GIỎ HÀNG CỦA BẠN" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <Button Click="BtnClose_Click" 
        HorizontalAlignment="Right" VerticalAlignment="Top" Margin="15"
        Width="40" Height="40"
        Background="#50000000"
        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <!-- Nền nút (Hình tròn hoặc bo góc) -->
                                <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="20">
                                    <!-- Nội dung (Icon) canh giữa -->
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <!-- [MỚI] Hiệu ứng khi di chuột: Đổi màu nền sang đỏ cam nhạt -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bdr" Property="Background" Value="{StaticResource AccentGradient}"/>
                                    <!-- Bạn có thể chỉnh màu khác, ví dụ "#AAEF4444"(đỏ cam) #80FFFFFF (trắng mờ) hoặc #AA000000 (đen mờ) -->
                                </Trigger>

                                <!-- Hiệu ứng khi nhấn giữ -->
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="bdr" Property="Opacity" Value="0.8"/>
                                    <Setter Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="0.95" ScaleY="0.95" CenterX="20" CenterY="20"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>

                    <!-- Icon X -->
                    <Path Data="{StaticResource IconClose}" Fill="White" Stretch="Uniform" Width="14" Height="14"/>
                </Button>
            </Grid>

            <!-- List Items -->
            <Border Grid.Row="1" Background="{StaticResource GlassBackground}" CornerRadius="15" Padding="10">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding GioHang}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#10FFFFFF" CornerRadius="10" Padding="10" Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="60"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Ảnh -->
                                        <Border CornerRadius="8" ClipToBounds="True" Height="60" Width="60">
                                            <Image Source="{Binding MonAn.HinhAnhUrl}" Stretch="UniformToFill"/>
                                        </Border>

                                        <!-- Thông tin -->
                                        <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding TenHienThi}" Foreground="White" FontWeight="SemiBold" FontSize="14"/>
                                            <TextBlock Text="{Binding GhiChu}" Foreground="#94A3B8" FontSize="12" FontStyle="Italic" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding ThanhTien, StringFormat='N0'}" Foreground="{StaticResource WarningYellow}" FontSize="13" FontWeight="Bold" Margin="0,2,0,0"/>
                                        </StackPanel>

                                        <!-- Số lượng -->
                                        <Border Grid.Column="2" Background="#20FFFFFF" CornerRadius="8" Padding="10,5" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding SoLuong, StringFormat='x{0}'}" Foreground="White" FontWeight="Bold"/>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <!-- Footer Actions -->
            <StackPanel Grid.Row="2" Margin="0,20,0,0">
                <Grid Margin="0,0,0,15">
                    <TextBlock Text="Tổng cộng:" Foreground="White" FontSize="16"/>
                    <TextBlock Text="{Binding TongTienCart, StringFormat='{}{0:N0} VNĐ'}" Foreground="{StaticResource SuccessGreen}" FontWeight="Bold" FontSize="20" HorizontalAlignment="Right"/>
                </Grid>

                <Button Content="GỬI ĐƠN ĐẾN BẾP" Click="BtnSubmit_Click" Style="{StaticResource GlassButtonStyle}" Background="{StaticResource SuccessGreen}" Height="50" FontSize="16" FontWeight="Bold"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>