<UserControl x:Class="OrMan.Helpers.LoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Border Visibility="{Binding IsLoading, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVis}}" 
            Background="#80000000" CornerRadius="{Binding CornerRadius, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- Spinner / ProgressBar -->
            <ProgressBar IsIndeterminate="True" Width="200" Height="4" 
                         Background="#20FFFFFF" 
                         Foreground="{DynamicResource AccentGradient}"/>

            <!-- Message -->
            <TextBlock Text="{Binding Message, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                       Foreground="White" 
                       Margin="0,15,0,0" 
                       FontSize="14"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Border>
</UserControl>